<!DOCTYPE html>
<html>
  <head>
    <title>Guro</title>
    <link href="assets/logo.png" rel="icon" type="image/png" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= render 'layouts/meta_header' %>
  </head>

  <body>
    <!-- <h1>you are using the LANDING layout!</h1> -->
    <br><br>
    <% if notice %>
      <div class="alert alert-primary alert-dismissible fade show mx-auto mt-5" role="alert" style="width: 75%">
        <p class="notice"><%= notice %></p>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-danger mx-auto mt-5" role="alert" style="width: 75%">
        <p class="alert"><%= alert %></p>
      </div>
    <% end %>
    
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
  
  <%= render 'layouts/meta_footer' %>
</html>
